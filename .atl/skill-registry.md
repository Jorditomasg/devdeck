# Skill Registry — devdeck

Generated by `sdd-init` on 2026-07-15. Regenerate with `/skill-registry` after installing/removing skills.

## Compact Rules (auto-inject into sub-agent prompts)

Source: `/mnt/c/Users/Jordi/PROYECTOS/devdeck/CLAUDE.md`.

- **Stack**: Tauri 2 desktop app. Rust core (`src-tauri/`) owns ALL side effects (process supervision via tokio, repo detection, git via CLI shell-outs, java discovery, profiles, docker compose, config persistence, tray, single-instance). Angular 22 frontend (`src/`) is zoneless, signals, standalone components, strict TS — pure UI over typed IPC, no business logic.
- **IPC**: 101 commands + 7 events documented in `docs/migration/ipc-contract.md` (count assertion in `src/app/core/ipc/commands.spec.ts`). Rust command names snake_case; wire arg keys camelCase. Error envelope: `{ kind, message }`. Wire names live in `core/ipc/commands.ts` (`CMD`) and `core/ipc/events.ts` (`EVT`), mirroring `src-tauri/src/events.rs` and `#[tauri::command]` registrations in `lib.rs` — update contract doc + both sides + count assertions together.
- **Layering (non-negotiable)**: `core/` (typed IPC wrappers, signal stores, i18n) → `ui/` (pure presentational, zero store/IPC imports) → `features/` (containers inject stores, translate ALL text). Containers translate; presentational components take plain inputs/outputs.
- **No ESM cycles**: `dialog-base.ts` must NEVER import `dialog.service.ts` (DI token pattern via `DIALOGS` in `dialog-stack.ts`, aliased in `app.config.ts`). Run `npx madge --circular --extensions ts src/app` before structural frontend changes.
- **Window handlers are main-only**: `on_window_event` in `lib.rs` early-returns for non-`"main"` labels — detached log/terminal/git windows (`log-*`, `term-*`, `git-*`) close/minimize normally; `frontend_ready` no-ops for non-main windows.
- **i18n**: every user-visible string via `t('key')` / `| t` pipe; keys in `src/assets/i18n/{en,es}.json` must keep identical key structure across both files.
- **Timing constants (do not lower)**: git badge poll 30s (concurrency cap 3, ONE shared semaphore between poller and on-demand queries), docker poll 15s, profile dirty debounce 300ms, log caps 500/service + 1000 global.
- **Repo detection is config-driven**: new framework support = new YAML under `config/repo-types/`, no code changes.
- **Process lifecycle**: processes spawn in their own process group (Unix `process_group(0)`, Windows `CREATE_NO_WINDOW` + job-style `taskkill /F /T`); stop escalates stop_cmd → SIGTERM (10s) → SIGKILL (5s) — deliberate, do not simplify.
- **Git subprocess layer**: `src-tauri/src/git/exec.rs` (shell-outs, `#[cfg(test)]` unit tests exist), `history.rs` (8-field log format incl. `%S` source; first-parent diffs — NEVER `diff-tree -m`), `parse.rs`, `ops.rs`, `poll.rs`, `worktree.rs` all have inline `#[cfg(test)] mod tests`. Every read shares the badge semaphore.
- **Windows→WSL git auto-routing**: repo path under `\\wsl.localhost\<distro>\...` or `\\wsl$` runs git inside the distro via `wsl.exe -d <distro> --cd <path> --exec git` (`git/exec.rs`) — `--exec` is mandatory (no shell, no injection). Service start/stop does NOT route through WSL (separate future feature).
- **Cargo.lock pin**: `time 0.3.47` — do not blindly `cargo update` (0.3.48 breaks `cookie 0.18.1`, E0119).
- **Commits**: conventional commits only, no AI attribution / Co-Authored-By trailers. Never `git commit`/`tag`/`push` on the user's behalf — hand back copy-pasteable commands.
- **Releases**: bump `package.json` + `src-tauri/tauri.conf.json` + `src-tauri/Cargo.toml` in lockstep, add `CHANGELOG.md` entry (user-facing, English, Keep a Changelog format), then tag `vX.Y.Z` (user pushes the tag; CI builds/signs the NSIS installer).

## Testing Conventions

- Rust: `cargo test --manifest-path src-tauri/Cargo.toml` — requires `npm run build` first (Tauri embeds the Angular dist). Unit tests live inline as `#[cfg(test)] mod tests` per module — confirmed present in `src-tauri/src/git/{exec,history,ops,parse,poll,worktree}.rs` and `src-tauri/src/wsl.rs`.
- Frontend: `npm test` → `vitest run` (vitest ^4.1.8, devDependency). No `@testing-library/*`, no e2e framework (playwright/cypress) detected — unit layer only on the frontend side.
- No coverage tool detected (no `--coverage` script, no `nyc`/`c8`/`tarpaulin` config).
- Type checking: `tsconfig.json` has `strict: true`, `strictInjectionParameters`, `strictInputAccessModifiers`, `strictTemplates` — Angular strict mode is the de facto type-checker gate (`ng build` / `tsc`).
- No ESLint config file found at project root. No `rustfmt.toml` / `clippy.toml` override files found (defaults apply if used).

## User Skills (trigger table)

Scanned `~/.claude/skills/*/SKILL.md` (deduplicated; project-level `.claude/skills/release` wins over any user-level `release`, none existed). `sdd-*`, `_shared`, `skill-registry` excluded per scan rules.

| Skill | Trigger |
|-------|---------|
| release (project, `.claude/skills/release`) | User asks to release a version, bump the version, or "saca la x.y.z" — bumps the three version files, writes CHANGELOG, commits/tags per CLAUDE.md release ritual. |
| tdd | Building features/fixing bugs with TDD; "red-green-refactor"; wants integration tests; test-first development. |
| diagnose | "diagnose this" / "debug this"; something broken/throwing/failing; performance regression. Reproduce → minimise → hypothesise → instrument → fix → regression-test. |
| judgment-day | "judgment day"/"dual review"/"juzgar" — parallel adversarial review via two blind judge sub-agents. |
| branch-pr | Creating a pull request / preparing changes for review (issue-first enforcement). |
| issue-creation | Creating a GitHub issue, reporting a bug, requesting a feature (issue-first enforcement). |
| grill-with-docs | Stress-testing a plan against the project's documented domain model/terminology; updates CONTEXT.md/ADRs inline. |
| caveman | "caveman mode" / "less tokens" / "be brief" — ultra-compressed communication. |
| go-testing | Go tests / Bubbletea TUI testing — NOT applicable to devdeck (no Go code); listed for completeness only. |
| skill-creator | Creating new AI agent skills. |

## Project Convention Files

- `/mnt/c/Users/Jordi/PROYECTOS/devdeck/CLAUDE.md` — primary index, fully read and inlined above.
- No `AGENTS.md`, `.cursorrules`, `GEMINI.md`, or `copilot-instructions.md` found at the project root.
